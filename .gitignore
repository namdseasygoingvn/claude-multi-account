# dependencies / build
node_modules/
dist/
dist-app/

# per-account Claude config dirs — live OAuth state, never commit
accounts/
accounts.json

# which account is currently rendered into the shared slot (runtime state)
switch-state.json

# scratch cwd used by spawned claude REPLs
.scratch/

# local captures from the spike script
captures/

.DS_Store
*.log
